# Runtime dependencies for the Streamlit policy simulator (app.py).
#
# Deliberately minimal. The app reads a pre-scored CSV rather than loading the
# model, so it needs no xgboost / scikit-learn / tensorflow at runtime — which
# is what keeps the deployed build small enough to fit a free hosting tier.
#
# To reproduce the modelling pipeline in the notebook, install
# requirements-notebook.txt instead.

streamlit>=1.45
pandas>=2.2
numpy>=2.1
plotly>=5.24
